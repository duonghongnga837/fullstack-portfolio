# 🚀 部署步骤指南

## ✅ 已完成的步骤

### 1. Git 配置 ✅
- ✅ Git 已安装
- ✅ 配置用户邮箱: duonghongnga837@gmail.com
- ✅ 配置用户名: duonghongnga837

### 2. 本地仓库初始化 ✅
- ✅ Git 仓库已初始化
- ✅ 所有文件已添加（77个文件，13,138行代码）
- ✅ 首次提交已完成

---

## 📋 接下来需要手动完成的步骤

### 第一步：在 GitHub 上创建仓库

#### 1. 登录 GitHub
```
🌐 访问: https://github.com
📧 邮箱: duonghongnga837@gmail.com
🔑 密码: jaUtqbCa123
```

#### 2. 创建新仓库
1. 点击右上角的 **"+"** 按钮
2. 选择 **"New repository"**
3. 填写仓库信息：
   - **Repository name**: `fullstack-portfolio`（或你喜欢的名字）
   - **Description**: `Full-stack portfolio project with React and Node.js`
   - **Public** 或 **Private**: 选择 Public（公开）
   - ⚠️ **不要勾选** "Add a README file"
   - ⚠️ **不要勾选** "Add .gitignore"
   - ⚠️ **不要勾选** "Choose a license"
4. 点击 **"Create repository"**

#### 3. 复制仓库地址
创建后，GitHub 会显示一个页面，复制 HTTPS 地址，格式类似：
```
https://github.com/duonghongnga837/fullstack-portfolio.git
```

---

### 第二步：推送代码到 GitHub

#### 在 PowerShell 中执行以下命令：

```powershell
# 1. 添加远程仓库（替换成你的仓库地址）
& "C:\Program Files\Git\bin\git.exe" remote add origin https://github.com/duonghongnga837/fullstack-portfolio.git

# 2. 重命名分支为 main
& "C:\Program Files\Git\bin\git.exe" branch -M main

# 3. 推送代码到 GitHub
& "C:\Program Files\Git\bin\git.exe" push -u origin main
```

⚠️ **注意**: 第一次推送时，Git 会要求你输入 GitHub 用户名和密码：
- **Username**: duonghongnga837@gmail.com
- **Password**: jaUtqbCa123

或者可能会弹出浏览器窗口让你登录 GitHub 授权。

---

### 第三步：部署后端到 Render

#### 1. 注册/登录 Render
```
🌐 访问: https://render.com
```
- 点击 **"Get Started"** 或 **"Sign Up"**
- 选择 **"Sign up with GitHub"**（推荐）
- 授权 Render 访问你的 GitHub 账号

#### 2. 创建 Web Service
1. 登录后，点击 **"New +"** → **"Web Service"**
2. 选择 **"Build and deploy from a Git repository"**
3. 点击 **"Connect account"** 连接 GitHub
4. 找到并选择 `fullstack-portfolio` 仓库
5. 点击 **"Connect"**

#### 3. 配置 Web Service
填写以下信息：

**Basic Settings:**
- **Name**: `portfolio-backend`（或你喜欢的名字）
- **Region**: 选择 `Singapore` 或 `Oregon`（离你最近的）
- **Branch**: `main`
- **Root Directory**: `Lab-17/Lab-17/nodemon-lab`
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`

**Instance Type:**
- 选择 **"Free"**（免费套餐）

**Environment Variables:**
点击 **"Add Environment Variable"** 添加以下变量：

1. **MONGO_URI**
   ```
   mongodb+srv://duonghongnga:jaUtqbCa123@cluster0.mongodb.net/portfolio?retryWrites=true&w=majority
   ```
   （从 `Lab-17/Lab-17/nodemon-lab/.env` 文件中复制）

2. **JWT_SECRET**
   ```
   your_jwt_secret_key_here_make_it_long_and_random
   ```
   （从 `Lab-17/Lab-17/nodemon-lab/.env` 文件中复制）

3. **PORT**
   ```
   3000
   ```

4. **NODE_ENV**
   ```
   production
   ```

#### 4. 部署
1. 点击 **"Create Web Service"**
2. 等待部署完成（约 3-5 分钟）
3. 部署成功后，你会看到一个 URL，类似：
   ```
   https://portfolio-backend.onrender.com
   ```
4. **复制这个 URL**，后面需要用到！

#### 5. 测试后端
访问以下地址测试：
```
https://portfolio-backend.onrender.com/api/projects
https://portfolio-backend.onrender.com/api/blog
```
应该能看到 JSON 数据。

---

### 第四步：部署前端到 Vercel

#### 1. 注册/登录 Vercel
```
🌐 访问: https://vercel.com
```
- 点击 **"Sign Up"**
- 选择 **"Continue with GitHub"**（推荐）
- 授权 Vercel 访问你的 GitHub 账号

#### 2. 导入项目
1. 登录后，点击 **"Add New..."** → **"Project"**
2. 找到并选择 `fullstack-portfolio` 仓库
3. 点击 **"Import"**

#### 3. 配置项目
填写以下信息：

**Project Settings:**
- **Framework Preset**: `Vite`（应该会自动检测）
- **Root Directory**: 点击 **"Edit"**，选择 `Lab-19/Lab-19/my-portfolio`
- **Build Command**: `npm run build`（默认）
- **Output Directory**: `dist`（默认）

**Environment Variables:**
点击 **"Add"** 添加环境变量：

1. **VITE_API_URL**
   ```
   https://portfolio-backend.onrender.com/api
   ```
   ⚠️ **重要**: 替换成你在第三步获得的 Render 后端 URL，并在末尾加上 `/api`

#### 4. 部署
1. 点击 **"Deploy"**
2. 等待部署完成（约 2-3 分钟）
3. 部署成功后，你会看到一个 URL，类似：
   ```
   https://fullstack-portfolio-xxx.vercel.app
   ```
4. 点击 **"Visit"** 访问你的网站！

---

## 🎉 部署完成！

### 你的网站地址：
- **前端**: https://fullstack-portfolio-xxx.vercel.app
- **后端**: https://portfolio-backend.onrender.com

### 测试账号：
```
📧 邮箱: admin@test.com
🔑 密码: password123
```

---

## ⚠️ 重要提示

### 1. Render 免费套餐限制
- ⏰ **15分钟无活动会休眠**
- 🐌 **休眠后首次访问需要 30-60 秒启动**
- 💡 **解决方案**: 使用 UptimeRobot 等服务定时 ping 你的后端

### 2. 环境变量检查
确保 Render 和 Vercel 的环境变量都正确配置：
- Render: MONGO_URI, JWT_SECRET, PORT, NODE_ENV
- Vercel: VITE_API_URL

### 3. CORS 配置
后端的 CORS 已配置为允许所有来源，如果需要限制，修改 `server.js`：
```javascript
app.use(cors({
  origin: 'https://fullstack-portfolio-xxx.vercel.app'
}));
```

---

## 🔧 常见问题

### Q1: 推送代码时要求输入密码，但密码不对？
**A**: GitHub 从 2021 年起不再支持密码认证，需要使用 Personal Access Token：
1. 访问 https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 勾选 `repo` 权限
4. 生成 token 并复制
5. 推送时，用户名输入邮箱，密码输入 token

### Q2: Render 部署失败？
**A**: 检查：
- Root Directory 是否正确设置为 `Lab-17/Lab-17/nodemon-lab`
- Build Command 是否为 `npm install`
- Start Command 是否为 `npm start`
- 环境变量是否都已添加

### Q3: Vercel 部署成功但页面空白？
**A**: 检查：
- Root Directory 是否正确设置为 `Lab-19/Lab-19/my-portfolio`
- VITE_API_URL 是否正确（包含 `/api` 后缀）
- 浏览器控制台是否有错误

### Q4: 前端无法连接后端？
**A**: 检查：
- Render 后端是否部署成功
- Vercel 的 VITE_API_URL 是否正确
- 后端 CORS 是否配置正确
- 后端是否从休眠中唤醒（等待 1 分钟）

---

## 📞 需要帮助？

如果遇到问题，检查：
1. **Render 日志**: 在 Render 控制台查看部署日志
2. **Vercel 日志**: 在 Vercel 控制台查看部署日志
3. **浏览器控制台**: 按 F12 查看错误信息
4. **网络请求**: 在浏览器 Network 标签查看 API 请求

---

## 🎯 下一步

部署完成后，你可以：
1. ✅ 测试所有功能
2. ✅ 自定义域名（Vercel 支持）
3. ✅ 设置 UptimeRobot 防止 Render 休眠
4. ✅ 添加 Google Analytics
5. ✅ 优化 SEO

**祝你部署顺利！** 🚀

